# Set JIRA issue's to have a field set to the build number of an xcode project

This is a Bitrise step to aid in CI. This step will find all the tickets that need to be updated to include the build number
